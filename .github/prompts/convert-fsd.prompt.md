---
name: convert-fsd
agent: FSD Converter
description: Convert FSD docx files in docs/fsd-source into Markdown in docs/fsd
---
Convert the FSDs as instructed. If I named a specific file in my message,
convert only that one; otherwise convert every .docx in docs/fsd-source that
needs it.